triguk_authentication:
    resource: "@trigukAuthenticationBundle/Controller/"
    type:     annotation
    prefix:   /

triguk_authorization:
    resource: "@trigukAuthorizationBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: "@AppBundle/Controller/"
    type:     annotation

logout:
    path: /logout

elfinder:
     resource: "@FMElfinderBundle/Resources/config/routing.yml"
